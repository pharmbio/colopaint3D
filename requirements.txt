# The one dependency list for everything in this repo: notebooks, run_all.py and
# utils/. Needs Python 3.10 — numba 0.56 / llvmlite 0.39 do not build above it.
#
#     python3.10 -m venv .venv
#     source .venv/bin/activate
#     pip install -r requirements.txt
#
# No package to install — helpers are imported from utils/ directly. Every entry is
# imported by code producing a paper panel, or pins one that is.

# --- Cell Painting pipeline -------------------------------------------------
pycytominer==0.2.0        # 2_Processing, normalisation and feature selection
cytominer-eval==0.1       # grit scores

# --- Core scientific stack --------------------------------------------------
pandas==1.5.2
numpy==1.22.0
scipy==1.7.3
scikit-learn==1.6.1
pyarrow==20.0.0           # the parquet engine behind every read_parquet
fastparquet==2022.12.0    # second parquet engine
polars==1.35.1            # 1_FeatureSorting, 3_CellCoverage

# --- Plotting ---------------------------------------------------------------
matplotlib==3.7.1
seaborn==0.13.2
adjustText==1.3.0         # label placement, Fig 6c
openpyxl==3.1.5           # utils/make_source_data.py
pymupdf                   # utils/check_panels_nonblank.py

# --- Dimensionality reduction (Fig 4, Suppl 4h) -----------------------------
# numba and llvmlite are not imported directly. They are pinned because
# umap-learn pulls numba, and anything newer than 0.56 requires numpy>=1.24.
umap-learn==0.5.3
numba==0.56.4
llvmlite==0.39.1
tqdm==4.67.1

# --- Gene-set enrichment (Fig 6d, Suppl 6b) ---------------------------------
# hallmark_nes_scatter.ipynb reruns the preranked GSEA behind those panels (~5 min).
# Without it, or without the MSigDB .gmt files, it falls back to the deposited
# prerank table and every panel still draws.
gseapy==1.3.1

# --- Notebook execution -----------------------------------------------------
# run_all.py prefers papermill and falls back to nbconvert; only nbconvert is
# declared. ipykernel is the kernel nbconvert runs notebooks in — without it every
# notebook dies at "Kernel died before replying to kernel_info".
nbconvert==7.16.6
ipykernel==6.30.0

# Not listed: scikit-image, needed only by the mask overlays in
# quantify_segmentation_error (diagnostics, not a paper panel). Install it yourself;
# unpinned it resolves to 0.25.x and drags numpy to 2.x, taking pycytominer with it.
