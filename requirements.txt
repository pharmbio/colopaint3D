# The one dependency list for everything in this repo: notebooks, run_all.py and
# scripts/. Needs Python 3.10 — numba 0.56 / llvmlite 0.39 do not build above it.
#
#     python3.10 -m venv .venv
#     source .venv/bin/activate
#     pip install -r requirements.txt
#
# There is no package to install — helpers are imported from utils/ directly.
#
# Every entry is imported by code that produces a paper panel, or is a version
# constraint holding one that is.

# --- Cell Painting pipeline -------------------------------------------------
pycytominer==0.2.0        # 2_Processing, normalisation and feature selection
cytominer-eval==0.1       # grit scores

# --- Core scientific stack --------------------------------------------------
pandas==1.5.2
numpy==1.22.0
scipy==1.7.3
scikit-learn==1.6.1
pyarrow==20.0.0           # the parquet engine behind every read_parquet
fastparquet==2022.12.0    # second parquet engine
polars==1.35.1            # 1_FeatureSorting, 3_CellCoverage

# --- Plotting ---------------------------------------------------------------
matplotlib==3.7.1
seaborn==0.13.2
adjustText==1.3.0         # label placement, Fig 6c
openpyxl==3.1.5           # scripts/make_source_data.py
pymupdf                   # scripts/check_panels_nonblank.py

# --- Dimensionality reduction (Fig 4, Suppl 4h) -----------------------------
# numba and llvmlite are not imported directly. They are pinned because
# umap-learn pulls numba, and anything newer than 0.56 requires numpy>=1.24.
umap-learn==0.5.3
numba==0.56.4
llvmlite==0.39.1
tqdm==4.67.1

# --- Notebook execution -----------------------------------------------------
# run_all.py prefers papermill and falls back to nbconvert; only nbconvert is
# declared, because it is the backend every run has actually used. ipykernel is
# not imported by anything — it is the kernel nbconvert executes notebooks in,
# and without it every notebook dies at "Kernel died before replying to
# kernel_info". Install papermill yourself if you want it.
nbconvert==7.16.6
ipykernel==6.30.0

# Dropped deliberately — nothing that makes a paper panel imports them:
#   copairs      -> mAP scoring; the mAP panel never reached the paper (0 imports)
#   gseapy       -> only hallmark_nes_scatter.ipynb, and only if
#                   data/gsea_prerank_results.csv is missing; that table is
#                   committed, and the cell it feeds is marked "not a paper panel"
#   statsmodels  -> was only ever pulled in transitively by copairs
#   scikit-image -> only the mask overlays in quantify_segmentation_error, guarded
#                   by a raw_images check and marked "not a paper panel"; Suppl 2d
#                   itself comes from the committed segmentation_iou_cached.csv.
#                   Dropping it also drops the worst pin here: unpinned it resolves
#                   to 0.25.x, which drags numpy to 2.x and scipy to 1.11 and takes
#                   numba and pycytominer down with it. Install it yourself to
#                   render those overlays.
#   tifffile, pillow -> the image-plotting code they served is not in the paper
#   nbformat     -> 0 imports; nbconvert pulls it in anyway
#   jupyter      -> meta-package dragging in jupyterlab, notebook and qtconsole;
#                   nbconvert and ipykernel are the only parts run_all.py needs
#   anndata, harmonypy, cuml, networkx, sqlalchemy, psycopg2-binary
#                -> single-cell/Harmony, GPU UMAP, network and database code, none
#                   of which is in this repo
